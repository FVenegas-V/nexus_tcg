# Ticket: Filtros por tipo de juego y categorías

- **ID del Ticket:** `fase2-0005`
- **Fase:** `Backend - Comunidades Base`
- **Estado:** `Pendiente por Implementar`
- **Prioridad:** `Baja`

---

## Descripción

Implementar un sistema robusto de categorización y filtrado de comunidades por tipo de juego, categorías temáticas y metadatos. Incluye APIs para obtener filtros disponibles y optimización de consultas.

---

## Criterios de Aceptación

- [ ] Modelo GameType para estandarizar tipos de juego
- [ ] Sistema de categorías jerárquicas para comunidades
- [ ] API GET /api/games/ para listado de juegos soportados
- [ ] API GET /api/categories/ para categorías disponibles
- [ ] Filtros avanzados en API de comunidades
- [ ] Sistema de tags dinámicos y sugerencias
- [ ] Búsqueda por combinaciones de filtros
- [ ] Estadísticas por categoría y juego
- [ ] Cache optimizado para filtros populares
- [ ] Interface de administración para gestionar tipos

---

## Detalles Técnicos y Notas de Implementación

**Modelos a implementar:**
- `backend/communities/models/game_type.py`
- `backend/communities/models/category.py`
- `backend/communities/models/tag.py`

**Modelo GameType:**
```python
class GameType(models.Model):
    name = models.CharField(max_length=100, unique=True)
    slug = models.SlugField(unique=True)
    description = models.TextField(blank=True)
    logo_url = models.URLField(blank=True, null=True)
    
    # Metadatos del juego
    publisher = models.CharField(max_length=100, blank=True)
    release_year = models.IntegerField(null=True, blank=True)
    min_players = models.IntegerField(default=2)
    max_players = models.IntegerField(null=True, blank=True)
    
    # Para ordenamiento y featured
    is_active = models.BooleanField(default=True)
    is_featured = models.BooleanField(default=False)
    community_count = models.IntegerField(default=0)
    
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        ordering = ['-is_featured', '-community_count', 'name']

class CommunityCategory(models.Model):
    name = models.CharField(max_length=50, unique=True)
    slug = models.SlugField(unique=True)
    description = models.TextField(blank=True)
    icon = models.CharField(max_length=50, blank=True)  # Material icon name
    color = models.CharField(max_length=7, default='#2196F3')  # Hex color
    
    # Jerarquía (opcional para futuro)
    parent = models.ForeignKey('self', on_delete=models.CASCADE, null=True, blank=True)
    
    # Estadísticas
    community_count = models.IntegerField(default=0)
    is_active = models.BooleanField(default=True)
    
    class Meta:
        verbose_name_plural = "categories"
        ordering = ['-community_count', 'name']

class CommunityTag(models.Model):
    name = models.CharField(max_length=30, unique=True)
    usage_count = models.IntegerField(default=0)
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        ordering = ['-usage_count', 'name']
```

**API Endpoints:**
```python
# Tipos de juego disponibles
GET /api/games/
Response:
{
  "featured": [
    {
      "id": 1,
      "name": "Magic: The Gathering",
      "slug": "magic-the-gathering",
      "logo_url": "https://...",
      "community_count": 45,
      "publisher": "Wizards of the Coast"
    }
  ],
  "all": [
    {
      "id": 1,
      "name": "Magic: The Gathering",
      "slug": "magic-the-gathering",
      "community_count": 45
    },
    {
      "id": 2,
      "name": "Pokemon TCG",
      "slug": "pokemon-tcg",
      "community_count": 32
    }
  ]
}

# Categorías disponibles
GET /api/categories/
Response:
[
  {
    "id": 1,
    "name": "Competitive",
    "slug": "competitive",
    "icon": "emoji_events",
    "color": "#FF5722",
    "community_count": 78
  },
  {
    "id": 2,
    "name": "Casual",
    "slug": "casual",
    "icon": "group",
    "color": "#4CAF50",
    "community_count": 156
  }
]

# Tags populares (para autocompletado)
GET /api/tags/
Query params:
- q: str (búsqueda de tags)
- limit: int (default 20)

Response:
[
  {
    "name": "competitive",
    "usage_count": 89
  },
  {
    "name": "beginner-friendly",
    "usage_count": 67
  }
]

# Estadísticas por filtros
GET /api/communities/stats/
Response:
{
  "total_communities": 234,
  "by_game_type": {
    "Magic: The Gathering": 89,
    "Pokemon TCG": 67,
    "Yu-Gi-Oh!": 45
  },
  "by_category": {
    "Competitive": 78,
    "Casual": 156
  },
  "by_difficulty": {
    "Principiante": 123,
    "Intermedio": 78,
    "Avanzado": 33
  }
}
```

**Filtros extendidos en comunidades:**
```python
# Actualizar CommunityFilter
class CommunityFilter(FilterSet):
    # Existentes
    search = CharFilter(method='filter_search')
    game_type = CharFilter(field_name='game_type__slug')
    category = NumberFilter(field_name='category__id')
    
    # Nuevos filtros
    game_type_name = CharFilter(field_name='game_type__name', lookup_expr='icontains')
    category_slug = CharFilter(field_name='category__slug')
    tags = CharFilter(method='filter_tags')
    min_members = NumberFilter(field_name='member_count', lookup_expr='gte')
    max_members = NumberFilter(field_name='member_count', lookup_expr='lte')
    created_after = DateFilter(field_name='created_at', lookup_expr='gte')
    is_featured_game = BooleanFilter(field_name='game_type__is_featured')
    
    def filter_tags(self, queryset, name, value):
        tag_list = [tag.strip() for tag in value.split(',')]
        return queryset.filter(tags__overlap=tag_list)
```

**Optimizaciones:**
```python
# Cache para filtros populares
@cache_result(timeout=300)  # 5 minutos
def get_game_types_with_stats():
    return GameType.objects.filter(is_active=True).annotate(
        community_count=Count('communities')
    ).order_by('-is_featured', '-community_count')

# Signals para mantener estadísticas
@receiver(post_save, sender=Community)
def update_game_type_stats(sender, instance, created, **kwargs):
    if created:
        instance.game_type.community_count = F('community_count') + 1
        instance.game_type.save(update_fields=['community_count'])
```

**Seeders/Fixtures:**
```python
# Juegos populares de TCG
GAME_TYPES = [
    {
        'name': 'Magic: The Gathering',
        'publisher': 'Wizards of the Coast',
        'release_year': 1993,
        'is_featured': True
    },
    {
        'name': 'Pokemon TCG',
        'publisher': 'The Pokemon Company',
        'release_year': 1996,
        'is_featured': True
    },
    # ... más juegos
]

# Categorías estándar
CATEGORIES = [
    {'name': 'Competitive', 'icon': 'emoji_events', 'color': '#FF5722'},
    {'name': 'Casual', 'icon': 'group', 'color': '#4CAF50'},
    {'name': 'Trading', 'icon': 'swap_horiz', 'color': '#2196F3'},
    {'name': 'Collecting', 'icon': 'collections', 'color': '#9C27B0'},
]
```

---

## Especificaciones Relacionadas

- `/docs/specs/06-categorization.md` - Sistema de categorización
- `/docs/apis/filtros.md` - APIs de filtros y búsqueda
- `/tickets/fase2-0002.md` - API de listado (para extender)

---

## Dependencias

- **Bloquea:** Ninguna (mejora de funcionalidad existente)
- **Bloqueado por:** `/tickets/fase2-0004.md` (Perfiles públicos)

---

## Sub-Tareas

- [ ] Crear modelos GameType, CommunityCategory, CommunityTag
- [ ] Migrar datos existentes a nuevo sistema
- [ ] Implementar GameTypeViewSet con estadísticas
- [ ] Crear CategoryViewSet con contador
- [ ] Desarrollar endpoint de tags con autocompletado
- [ ] Extender filtros en CommunityFilter
- [ ] Implementar cache para consultas frecuentes
- [ ] Crear fixtures con datos realistas
- [ ] Desarrollar signals para estadísticas automáticas
- [ ] Optimizar queries con índices
- [ ] Crear tests para filtros complejos
- [ ] Actualizar documentación de APIs

---

## Comentarios y Discusión

Este sistema debe ser flexible para agregar nuevos juegos TCG y categorías sin afectar funcionalidad existente. Las estadísticas deben actualizarse eficientemente.
